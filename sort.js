// sorting with string
const month =['may', 'jun', 'april','jan'];
console.log(month.sort());
//sorting with numbers
const number = [1, 5, 25, 100, 21];
console.log(number.sort());// sort method work with only string 
//number convert in string then short 
//in number convert in string the 25 is bigget the 100 and 1 is biger
// then 2