for(var num = 1; num<=10;num++){
    var tableOf = 12;
    // console.log(tableOf +" * "+num + " = " + tableOf * num);
    console.log(`${tableOf} * ${num} = ${tableOf+num}`);//templates
    //literals
}